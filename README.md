# asset-forge
asset-forge is an open-source collection of browser-based generators for GitHub profile assets. Each tool is a self-contained HTML file — no build step, no server, no dependencies. Open it locally or via GitHub Pages, customize your asset, and get the output.
